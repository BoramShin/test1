import sys

class FASTA:
    def __init__(self, file_name: str):
        self.file_name = file_name
        self.count = {}
    
    def count_read_num(self):
        with open(slef.file_name,'r') as h
            for line in handle:
                if line.startwith(">"):
                    continue
                line = line.strip()
                for s in lines:
                    if s in self.count:
                        self.count[s]   
